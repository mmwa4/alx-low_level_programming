#include<stdio.h>
/**
 * main - This is where the compiler will start, its the entry point.
 * Return: 0 which prints: Programming is like building a multilingual puzzle
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
