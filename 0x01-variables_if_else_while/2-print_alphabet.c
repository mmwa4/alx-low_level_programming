#include <stdio.h>
/**
 * main - This script prints the letters of the alphabet
 * Return: 0 Successful execution of script
 */

int main(void)

{
	char l;

	for (l = 'a'; l <= 'z'; l++)
	putchar(l);
	putchar('\n');
	return (0);
}
