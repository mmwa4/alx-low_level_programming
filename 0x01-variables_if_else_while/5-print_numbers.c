#include <time.h>
#include <stdlib.h>
#include <stdio.h>

/**
 * main - This is where the code is first executed
 * Return - Returns 0 when code is successfully executed
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
		printf("\n");
		return (0);

}
